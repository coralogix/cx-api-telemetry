# cx-api-telemetry

This is a repository that contains the Protobuf message and service definitions for the Coralogix telemetry API. That API is used by `coralogix-aws-lambda-telemetry-exporter` to deliver all lambda telemetry in a single request.
